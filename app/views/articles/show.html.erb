<h1><%= @article.title %></h1>
<p><%= link_to 'Edit', edit_article_path(@article) %> | <%= link_to 'Delete', article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %></p>
<p><%= @article.content %></p>


<%= link_to 'Return', articles_path %>
